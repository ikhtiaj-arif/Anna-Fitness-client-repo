import React from 'react';

const ProgramCard = ({ program }) => {
    // const {title, image, details, info}
    return (
        <div>
            
        </div>
    );
};

export default ProgramCard;